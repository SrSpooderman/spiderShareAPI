server {
  listen 80;
  server_name ${API_DOMAIN} ${BACKOFFICE_DOMAIN};

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 200 "SpiderShare ACME challenge server\n";
    add_header Content-Type text/plain;
  }
}
